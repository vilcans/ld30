describe 'Give it some context', ->
    describe 'maybe a bit more context here', ->
        it 'should run here few assertions', ->
            assert.equal 1 + 1, 2
            #'hello'.should.equal 'foo'
